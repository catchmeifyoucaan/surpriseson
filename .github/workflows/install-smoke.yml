name: Install Smoke

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  install-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout CLI
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10
      - name: Enable Corepack
        run: corepack enable

      - name: Install pnpm deps (minimal)
        run: pnpm install --ignore-scripts --frozen-lockfile

      - name: Run installer docker tests
        env:
          SURPRISEBOT_INSTALL_URL: https://surprisebot.bot/install.sh
          SURPRISEBOT_INSTALL_CLI_URL: https://surprisebot.bot/install-cli.sh
          SURPRISEBOT_NO_ONBOARD: "1"
          SURPRISEBOT_INSTALL_SMOKE_SKIP_CLI: "1"
          SURPRISEBOT_INSTALL_SMOKE_PREVIOUS: "2026.1.11-4"
        run: pnpm test:install:smoke
